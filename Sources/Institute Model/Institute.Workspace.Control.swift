extension Institute.Workspace {
    public enum Control: Swift.String, CaseIterable, Sendable {
        case application
        case institute
        case continuousIntegration = "continuous-integration"
    }
}
