// swift-tools-version: 6.3.3

import PackageDescription

let package = Package(
    name: "D",
    products: [
        .library(name: "D", targets: ["D"])
    ],
    targets: [
        .target(name: "D")
    ]
)
